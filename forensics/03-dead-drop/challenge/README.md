# Dead Drop

During the AX-07 incident, investigators recovered a suspicious file
from a workstation used by the reconnaissance team.

The file appears to be an ordinary binary transfer, but something
doesn't quite add up.

Investigate the artifact and determine what was deliberately left behind.

**File:** `kinetica_drop.bin`

**Flag format:** `snuc{...}`
